# OpenClaw Adapter

OpenClaw-specific integration notes for the generic PagePop skill.

This adapter may document:

- OpenClaw installation flow.
- Launch-context fields.
- Event rendering expectations.
- Authorization return guidance.

Generic PagePop behavior belongs in `packages/pagepop-core` and `packages/pagepop-skill`.

This directory must not contain non-production hostnames or package assets.
