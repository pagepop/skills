# pagepop-mcp-server

MCP server for PagePop.

This package will expose PagePop capabilities as MCP tools, resources, and prompts while reusing `pagepop-core` for API behavior.

Transport-specific code belongs here. PagePop API behavior should stay in `pagepop-core`.
